# Frontend placeholder

This folder will contain the React frontend (Vite) for the SIAC-IoT project.

For quick start (later):
- npx create-vite@latest frontend --template react
- cd frontend
- npm install
- npm run dev
